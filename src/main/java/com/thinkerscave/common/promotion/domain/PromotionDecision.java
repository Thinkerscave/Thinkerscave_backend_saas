package com.thinkerscave.common.promotion.domain;

/** Per-student decision inside a promotion batch. */
public enum PromotionDecision {
    PROMOTED,
    RETAINED,
    GRADUATED,
    TRANSFERRED_OUT,
    WITHHELD
}
